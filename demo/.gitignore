# Prebuild output. Regenerated by `expo prebuild` (locally) or by EAS on
# every build; never commit. The library's own android/ and ios/ at the
# repo root ARE committed because they ship in the npm tarball.
android/
ios/

# Expo / Metro caches
.expo/
.expo-shared/
dist/
web-build/
